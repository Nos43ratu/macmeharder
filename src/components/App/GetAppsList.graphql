query GetAppsList($page:Int,$count:Int,$category:Int) {
    appsList(page: $page,count: $count,category: $category){
        id
        title
        appAvatar {
            url
        }
        description
        category{
            id
            name
        }
        appImages{
            url
        }
    }
}