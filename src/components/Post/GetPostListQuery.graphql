query GetPostList($page:Int,$count:Int,$category:Int!) {
    postList(page: $page,count:$count,category: $category){
        id
        title
        miniTitle
        body
        postImages {
            url
        }
    }
}